# HeartSignal v2.0 - 개선사항 및 최적화 🚀

> **"완성도 높은 사용자 경험, 견고한 아키텍처"** - HeartSignal v2.0의 핵심 목표

## 📋 v1.0 대비 주요 개선사항 요약

### 🔧 Critical Issues 해결
- **Missing Dependencies**: 필수 라이브러리 및 컴포넌트 구현
- **Button Functionality**: 모든 버튼의 실제 동작 구현
- **Context Integration**: AppContext 프로바이더 통합
- **Type Safety**: 완전한 TypeScript 타입 지원

### ✨ UX/UI 개선사항
- **Progressive Loading**: 점진적 콘텐츠 로딩
- **Error Boundaries**: 견고한 에러 처리
- **Accessibility**: WCAG 2.1 AA 준수
- **Performance**: Core Web Vitals 최적화

---

## 🚨 v1.0에서 발견된 Critical Issues

### 1. Missing Core Files
```bash
# 누락된 핵심 파일들
src/lib/design-tokens.ts     # ❌ 존재하지 않음
src/lib/theme.ts            # ❌ 존재하지 않음  
src/components/animation/Stagger.tsx  # ❌ 존재하지 않음
```

### 2. Non-functional Buttons
```typescript
// ❌ 현재 상태 - 동작하지 않는 버튼들
<Button size="lg" className="bg-primary-500 hover:bg-primary-600">
  <Play className="h-5 w-5 mr-2" />
  지금 시작하기  {/* onClick 핸들러 없음 */}
</Button>

<Button variant="outline" size="lg">
  서비스 둘러보기  {/* onClick 핸들러 없음 */}
</Button>
```

### 3. Context Provider Integration
```typescript
// ❌ 현재 layout.tsx - AppProvider 누락
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ko">
      <body className={inter.className}>
        {children}  {/* AppProvider로 감싸지지 않음 */}
      </body>
    </html>
  )
}
```

---

## ✅ v2.0 해결방안 및 구현사항

### Phase 1: Core Infrastructure 완성 🏗️

#### 1.1 Missing Files 생성
```typescript
// src/lib/design-tokens.ts - v1.md 명세 그대로 구현
export const designTokens = {
  colors: {
    primary: {
      50: '#FDF2F8',
      100: '#FCE7F3', 
      500: '#E95877',  // Main brand color
      600: '#D946A0',
      900: '#831843',
    },
    semantic: {
      success: '#2FBF71',
      warning: '#F59E0B', 
      danger: '#E11D48',
      info: '#3B82F6',
    }
  },
  // ... v1.md 전체 토큰 시스템 구현
} as const;

// src/lib/theme.ts - 테마 시스템 구현
export interface Theme {
  name: 'light' | 'dark';
  colors: typeof designTokens.colors & {
    neutral: Record<string, string>;
    white: string;
    black: string;
  };
}

export const lightTheme: Theme = { /* 구현 */ };
export const darkTheme: Theme = { /* 구현 */ };
```

#### 1.2 Missing Animation Components
```typescript
// src/components/animation/Stagger.tsx
interface StaggerProps {
  staggerDelay?: number;
  className?: string;
  children: React.ReactNode[];
}

export function Stagger({ 
  staggerDelay = 0.1, 
  className, 
  children 
}: StaggerProps) {
  return (
    <div className={className}>
      {React.Children.map(children, (child, index) => (
        <motion.div
          key={index}
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ 
            duration: 0.5,
            delay: index * staggerDelay,
            ease: "easeOut"
          }}
        >
          {child}
        </motion.div>
      ))}
    </div>
  );
}
```

#### 1.3 Context Provider Integration
```typescript
// src/app/layout.tsx - 수정된 버전
import { AppProvider } from '@/contexts/AppContext';

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ko">
      <body className={inter.className}>
        <AppProvider>
          {children}
        </AppProvider>
      </body>
    </html>
  )
}
```

### Phase 2: Button Functionality 구현 🎯

#### 2.1 Hero Section Button Actions
```typescript
// src/app/page.tsx - 개선된 Hero 섹션
export default function Home() {
  const router = useRouter();
  const controller = useAnalysisController();

  const handleStartAnalysis = useCallback(() => {
    // 분석 섹션으로 스크롤
    const analysisSection = document.getElementById('analysis-demo');
    analysisSection?.scrollIntoView({ 
      behavior: 'smooth',
      block: 'start'
    });
    
    // 자동으로 세션 시작 (선택사항)
    // controller.startSession();
  }, []);

  const handleServiceTour = useCallback(() => {
    // 서비스 투어 모달 또는 가이드 시작
    setShowServiceTour(true);
  }, []);

  return (
    <div className="min-h-screen flex flex-col">
      {/* Hero Section */}
      <section className="py-16 md:py-24 bg-gradient-to-br from-primary-50 to-secondary-50">
        <Container>
          <FadeIn className="text-center space-y-6">
            {/* ... 기존 내용 ... */}
            
            <div className="flex flex-col sm:flex-row items-center justify-center gap-4">
              <Button 
                size="lg" 
                onClick={handleStartAnalysis}
                className="bg-primary-500 hover:bg-primary-600 text-white px-8 py-3"
              >
                <Play className="h-5 w-5 mr-2" />
                지금 시작하기
              </Button>
              <Button 
                variant="outline" 
                size="lg" 
                onClick={handleServiceTour}
                className="px-8 py-3"
              >
                서비스 둘러보기
              </Button>
            </div>
          </FadeIn>
        </Container>
      </section>

      {/* Demo Section - ID 추가 */}
      <section id="analysis-demo" className="py-16 md:py-24">
        {/* ... 기존 내용 ... */}
      </section>
    </div>
  );
}
```

#### 2.2 ControlsBar Enhanced Functionality
```typescript
// src/components/ControlsBar.tsx - 개선된 버전
interface ControlsBarProps {
  onReset: () => void;
  onSave?: () => void;
  onShare?: () => void;
  disabled?: boolean;
  // v2.0 추가 기능
  sessionData?: SessionData;
  showExport?: boolean;
}

export function ControlsBar({ 
  onReset, 
  onSave, 
  onShare, 
  disabled = false,
  sessionData,
  showExport = false
}: ControlsBarProps) {
  
  const handleSave = useCallback(async () => {
    if (!sessionData || !onSave) return;
    
    try {
      // 로컬 스토리지에 저장
      const savedSessions = JSON.parse(
        localStorage.getItem('heartsignal-sessions') || '[]'
      );
      
      const newSession = {
        id: Date.now().toString(),
        timestamp: new Date().toISOString(),
        data: sessionData,
      };
      
      savedSessions.push(newSession);
      localStorage.setItem('heartsignal-sessions', JSON.stringify(savedSessions));
      
      // 사용자에게 피드백
      toast.success('분석 결과가 저장되었습니다!');
      
      onSave();
    } catch (error) {
      toast.error('저장 중 오류가 발생했습니다.');
    }
  }, [sessionData, onSave]);

  const handleShare = useCallback(async () => {
    if (!sessionData || !onShare) return;
    
    try {
      // Web Share API 지원 확인
      if (navigator.share) {
        await navigator.share({
          title: 'HeartSignal 분석 결과',
          text: `매칭 점수: ${sessionData.match?.score}점`,
          url: window.location.href,
        });
      } else {
        // 폴백: 클립보드 복사
        await navigator.clipboard.writeText(
          `HeartSignal 분석 결과\n매칭 점수: ${sessionData.match?.score}점\n${window.location.href}`
        );
        toast.success('링크가 클립보드에 복사되었습니다!');
      }
      
      onShare();
    } catch (error) {
      toast.error('공유 중 오류가 발생했습니다.');
    }
  }, [sessionData, onShare]);

  const handleExport = useCallback(() => {
    if (!sessionData) return;
    
    // JSON 파일로 내보내기
    const dataStr = JSON.stringify(sessionData, null, 2);
    const dataUri = 'data:application/json;charset=utf-8,'+ encodeURIComponent(dataStr);
    
    const exportFileDefaultName = `heartsignal-analysis-${new Date().toISOString().split('T')[0]}.json`;
    
    const linkElement = document.createElement('a');
    linkElement.setAttribute('href', dataUri);
    linkElement.setAttribute('download', exportFileDefaultName);
    linkElement.click();
    
    toast.success('분석 데이터가 다운로드되었습니다!');
  }, [sessionData]);

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.5, delay: 0.3 }}
      className="flex items-center justify-center space-x-3 flex-wrap gap-2"
    >
      {/* 다시 시도 */}
      <Button
        onClick={onReset}
        disabled={disabled}
        variant="outline"
        size="lg"
        className="flex items-center space-x-2 px-6 py-3 rounded-full border-2 hover:bg-gray-50 transition-all duration-200"
      >
        <RotateCcw className="w-4 h-4" />
        <span>다시 시도</span>
      </Button>

      {/* 결과 저장 */}
      {onSave && (
        <Button
          onClick={handleSave}
          disabled={disabled || !sessionData}
          variant="outline"
          size="lg"
          className="flex items-center space-x-2 px-6 py-3 rounded-full border-2 hover:bg-gray-50 transition-all duration-200"
        >
          <Download className="w-4 h-4" />
          <span>결과 저장</span>
        </Button>
      )}

      {/* 내보내기 (v2.0 신규) */}
      {showExport && sessionData && (
        <Button
          onClick={handleExport}
          disabled={disabled}
          variant="outline"
          size="lg"
          className="flex items-center space-x-2 px-6 py-3 rounded-full border-2 hover:bg-gray-50 transition-all duration-200"
        >
          <FileDown className="w-4 h-4" />
          <span>내보내기</span>
        </Button>
      )}

      {/* 공유하기 */}
      {onShare && (
        <Button
          onClick={handleShare}
          disabled={disabled || !sessionData}
          size="lg"
          className="flex items-center space-x-2 px-6 py-3 rounded-full bg-[#E95877] hover:bg-[#F26B88] text-white transition-all duration-200"
        >
          <Share2 className="w-4 h-4" />
          <span>공유하기</span>
        </Button>
      )}
    </motion.div>
  );
}
```

### Phase 3: UX/UI 개선사항 ✨

#### 3.1 Progressive Loading System
```typescript
// src/components/ProgressiveLoader.tsx - 새로운 컴포넌트
interface ProgressiveLoaderProps {
  phase: SessionPhase;
  progress?: number;
  message?: string;
}

export function ProgressiveLoader({ 
  phase, 
  progress = 0, 
  message 
}: ProgressiveLoaderProps) {
  const getPhaseMessage = (phase: SessionPhase) => {
    switch (phase) {
      case 'recording': return '대화를 녹음하고 있어요...';
      case 'processing': return 'AI가 대화를 분석하고 있어요...';
      case 'done': return '분석이 완료되었어요!';
      default: return '준비 중이에요...';
    }
  };

  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      className="text-center space-y-4"
    >
      {/* 진행률 바 */}
      <div className="w-full max-w-md mx-auto bg-gray-200 rounded-full h-2">
        <motion.div
          className="bg-primary-500 h-2 rounded-full"
          initial={{ width: 0 }}
          animate={{ width: `${progress}%` }}
          transition={{ duration: 0.5, ease: "easeOut" }}
        />
      </div>
      
      {/* 메시지 */}
      <p className="text-sm text-gray-600">
        {message || getPhaseMessage(phase)}
      </p>
      
      {/* 로딩 애니메이션 */}
      {phase === 'processing' && (
        <div className="flex justify-center space-x-1">
          {[0, 1, 2].map((i) => (
            <motion.div
              key={i}
              className="w-2 h-2 bg-primary-500 rounded-full"
              animate={{ scale: [1, 1.2, 1] }}
              transition={{
                duration: 1,
                repeat: Infinity,
                delay: i * 0.2,
              }}
            />
          ))}
        </div>
      )}
    </motion.div>
  );
}
```

#### 3.2 Service Tour Modal
```typescript
// src/components/ServiceTour.tsx - 새로운 컴포넌트
interface ServiceTourProps {
  isOpen: boolean;
  onClose: () => void;
}

const tourSteps = [
  {
    title: '실시간 대화 분석',
    description: '마이크를 통해 대화를 녹음하면 AI가 실시간으로 분석합니다.',
    image: '/images/tour-step-1.png',
  },
  {
    title: '감정 및 라포 측정',
    description: '대화의 감정 상태와 상호간의 라포 형성도를 측정합니다.',
    image: '/images/tour-step-2.png',
  },
  {
    title: '매칭 점수 및 피드백',
    description: '0-100점 매칭 점수와 구체적인 개선 방안을 제공합니다.',
    image: '/images/tour-step-3.png',
  },
];

export function ServiceTour({ isOpen, onClose }: ServiceTourProps) {
  const [currentStep, setCurrentStep] = useState(0);
  
  const nextStep = () => {
    if (currentStep < tourSteps.length - 1) {
      setCurrentStep(currentStep + 1);
    } else {
      onClose();
    }
  };
  
  const prevStep = () => {
    if (currentStep > 0) {
      setCurrentStep(currentStep - 1);
    }
  };

  return (
    <Dialog open={isOpen} onOpenChange={onClose}>
      <DialogContent className="max-w-2xl">
        <DialogHeader>
          <DialogTitle>HeartSignal 서비스 투어</DialogTitle>
          <DialogDescription>
            {currentStep + 1} / {tourSteps.length}
          </DialogDescription>
        </DialogHeader>
        
        <div className="space-y-6">
          {/* 이미지 */}
          <div className="aspect-video bg-gray-100 rounded-lg flex items-center justify-center">
            <img 
              src={tourSteps[currentStep].image} 
              alt={tourSteps[currentStep].title}
              className="max-w-full max-h-full object-contain"
            />
          </div>
          
          {/* 내용 */}
          <div className="text-center space-y-2">
            <h3 className="text-xl font-semibold">
              {tourSteps[currentStep].title}
            </h3>
            <p className="text-gray-600">
              {tourSteps[currentStep].description}
            </p>
          </div>
          
          {/* 진행률 인디케이터 */}
          <div className="flex justify-center space-x-2">
            {tourSteps.map((_, index) => (
              <div
                key={index}
                className={`w-2 h-2 rounded-full transition-colors ${
                  index === currentStep ? 'bg-primary-500' : 'bg-gray-300'
                }`}
              />
            ))}
          </div>
        </div>
        
        <DialogFooter className="flex justify-between">
          <Button 
            variant="outline" 
            onClick={prevStep}
            disabled={currentStep === 0}
          >
            이전
          </Button>
          <Button onClick={nextStep}>
            {currentStep === tourSteps.length - 1 ? '완료' : '다음'}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

#### 3.3 Enhanced Error Boundary
```typescript
// src/components/ErrorBoundary.tsx - 개선된 에러 바운더리
interface ErrorBoundaryState {
  hasError: boolean;
  error?: Error;
  errorInfo?: ErrorInfo;
}

export class ErrorBoundary extends Component<
  { children: ReactNode; fallback?: ComponentType<ErrorBoundaryProps> },
  ErrorBoundaryState
> {
  constructor(props: any) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: ErrorInfo) {
    this.setState({ errorInfo });
    
    // 에러 로깅 (Sentry, LogRocket 등)
    console.error('ErrorBoundary caught an error:', error, errorInfo);
    
    // 사용자에게 피드백
    toast.error('앱에서 오류가 발생했습니다. 페이지를 새로고침해주세요.');
  }

  render() {
    if (this.state.hasError) {
      const FallbackComponent = this.props.fallback || DefaultErrorFallback;
      return (
        <FallbackComponent
          error={this.state.error}
          errorInfo={this.state.errorInfo}
          resetError={() => this.setState({ hasError: false })}
        />
      );
    }

    return this.props.children;
  }
}

// 기본 에러 폴백 컴포넌트
function DefaultErrorFallback({ error, resetError }: ErrorBoundaryProps) {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <Card className="max-w-md w-full mx-4">
        <CardHeader>
          <CardTitle className="flex items-center space-x-2 text-red-600">
            <AlertCircle className="h-5 w-5" />
            <span>오류가 발생했습니다</span>
          </CardTitle>
        </CardHeader>
        <CardContent className="space-y-4">
          <p className="text-gray-600">
            예상치 못한 오류가 발생했습니다. 불편을 드려 죄송합니다.
          </p>
          {process.env.NODE_ENV === 'development' && error && (
            <details className="text-sm bg-gray-100 p-3 rounded">
              <summary>에러 세부사항</summary>
              <pre className="mt-2 whitespace-pre-wrap">{error.message}</pre>
            </details>
          )}
          <div className="flex space-x-2">
            <Button onClick={resetError} variant="outline" size="sm">
              다시 시도
            </Button>
            <Button 
              onClick={() => window.location.reload()} 
              size="sm"
            >
              페이지 새로고침
            </Button>
          </div>
        </CardContent>
      </Card>
    </div>
  );
}
```

### Phase 4: Performance & Accessibility 최적화 ⚡

#### 4.1 Core Web Vitals 최적화
```typescript
// src/lib/performance.ts - 성능 모니터링
export function measureWebVitals() {
  if (typeof window !== 'undefined') {
    import('web-vitals').then(({ getCLS, getFID, getFCP, getLCP, getTTFB }) => {
      getCLS(console.log);
      getFID(console.log);
      getFCP(console.log);
      getLCP(console.log);
      getTTFB(console.log);
    });
  }
}

// 이미지 최적화
export function OptimizedImage({ 
  src, 
  alt, 
  className,
  ...props 
}: ImageProps) {
  return (
    <Image
      src={src}
      alt={alt}
      className={className}
      loading="lazy"
      placeholder="blur"
      blurDataURL="data:image/jpeg;base64,"
      {...props}
    />
  );
}
```

#### 4.2 Accessibility 강화
```typescript
// src/components/AccessibilityFeatures.tsx
export function AccessibilityFeatures() {
  const [reducedMotion, setReducedMotion] = useState(false);
  const [highContrast, setHighContrast] = useState(false);
  
  useEffect(() => {
    // Reduced motion 감지
    const mediaQuery = window.matchMedia('(prefers-reduced-motion: reduce)');
    setReducedMotion(mediaQuery.matches);
    
    const handleChange = (e: MediaQueryListEvent) => {
      setReducedMotion(e.matches);
    };
    
    mediaQuery.addEventListener('change', handleChange);
    return () => mediaQuery.removeEventListener('change', handleChange);
  }, []);

  return (
    <div className="sr-only">
      <button 
        onClick={() => document.getElementById('main-content')?.focus()}
        className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 bg-primary-500 text-white px-4 py-2 rounded"
      >
        메인 콘텐츠로 건너뛰기
      </button>
    </div>
  );
}

// 키보드 내비게이션 강화
export function useKeyboardNavigation() {
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      // Escape 키로 모달 닫기
      if (e.key === 'Escape') {
        const openModal = document.querySelector('[role="dialog"]');
        if (openModal) {
          const closeButton = openModal.querySelector('[data-close-modal]') as HTMLElement;
          closeButton?.click();
        }
      }
      
      // Tab 키 트랩핑 (모달 내부에서)
      if (e.key === 'Tab') {
        const modal = document.querySelector('[role="dialog"]');
        if (modal) {
          const focusableElements = modal.querySelectorAll(
            'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
          );
          
          if (focusableElements.length > 0) {
            const firstElement = focusableElements[0] as HTMLElement;
            const lastElement = focusableElements[focusableElements.length - 1] as HTMLElement;
            
            if (e.shiftKey && document.activeElement === firstElement) {
              e.preventDefault();
              lastElement.focus();
            } else if (!e.shiftKey && document.activeElement === lastElement) {
              e.preventDefault();
              firstElement.focus();
            }
          }
        }
      }
    };
    
    document.addEventListener('keydown', handleKeyDown);
    return () => document.removeEventListener('keydown', handleKeyDown);
  }, []);
}
```

### Phase 5: Advanced Features 🔮

#### 5.1 Session History Management
```typescript
// src/features/history/components/SessionHistory.tsx
interface SessionHistoryProps {
  sessions: SavedSession[];
  onLoadSession: (session: SavedSession) => void;
  onDeleteSession: (id: string) => void;
}

export function SessionHistory({ 
  sessions, 
  onLoadSession, 
  onDeleteSession 
}: SessionHistoryProps) {
  const [sortBy, setSortBy] = useState<'date' | 'score'>('date');
  const [filterBy, setFilterBy] = useState<'all' | 'high' | 'medium' | 'low'>('all');
  
  const filteredSessions = useMemo(() => {
    let filtered = sessions;
    
    // 필터링
    if (filterBy !== 'all') {
      filtered = sessions.filter(session => {
        const score = session.data.match?.score || 0;
        switch (filterBy) {
          case 'high': return score >= 80;
          case 'medium': return score >= 60 && score < 80;
          case 'low': return score < 60;
          default: return true;
        }
      });
    }
    
    // 정렬
    return filtered.sort((a, b) => {
      if (sortBy === 'date') {
        return new Date(b.timestamp).getTime() - new Date(a.timestamp).getTime();
      } else {
        const scoreA = a.data.match?.score || 0;
        const scoreB = b.data.match?.score || 0;
        return scoreB - scoreA;
      }
    });
  }, [sessions, sortBy, filterBy]);

  return (
    <div className="space-y-6">
      {/* 필터 및 정렬 */}
      <div className="flex flex-col sm:flex-row gap-4 items-start sm:items-center justify-between">
        <h2 className="text-2xl font-bold">분석 히스토리</h2>
        
        <div className="flex gap-2">
          <Select value={sortBy} onValueChange={setSortBy}>
            <SelectTrigger className="w-32">
              <SelectValue />
            </SelectTrigger>
            <SelectContent>
              <SelectItem value="date">날짜순</SelectItem>
              <SelectItem value="score">점수순</SelectItem>
            </SelectContent>
          </Select>
          
          <Select value={filterBy} onValueChange={setFilterBy}>
            <SelectTrigger className="w-32">
              <SelectValue />
            </SelectTrigger>
            <SelectContent>
              <SelectItem value="all">전체</SelectItem>
              <SelectItem value="high">높음 (80+)</SelectItem>
              <SelectItem value="medium">보통 (60-79)</SelectItem>
              <SelectItem value="low">낮음 (60미만)</SelectItem>
            </SelectContent>
          </Select>
        </div>
      </div>
      
      {/* 세션 목록 */}
      <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
        {filteredSessions.map((session) => (
          <SessionHistoryCard
            key={session.id}
            session={session}
            onLoad={() => onLoadSession(session)}
            onDelete={() => onDeleteSession(session.id)}
          />
        ))}
      </div>
      
      {filteredSessions.length === 0 && (
        <div className="text-center py-12">
          <p className="text-gray-500">
            {sessions.length === 0 
              ? '아직 저장된 분석 결과가 없습니다.' 
              : '필터 조건에 맞는 결과가 없습니다.'
            }
          </p>
        </div>
      )}
    </div>
  );
}
```

#### 5.2 Real-time Collaboration (미래 기능)
```typescript
// src/features/collaboration/hooks/useRealTimeSession.ts
export function useRealTimeSession(sessionId: string) {
  const [participants, setParticipants] = useState<Participant[]>([]);
  const [isConnected, setIsConnected] = useState(false);
  
  useEffect(() => {
    // WebSocket 연결 (미래 구현)
    const ws = new WebSocket(`wss://api.heartsignal.com/sessions/${sessionId}`);
    
    ws.onopen = () => setIsConnected(true);
    ws.onclose = () => setIsConnected(false);
    
    ws.onmessage = (event) => {
      const data = JSON.parse(event.data);
      
      switch (data.type) {
        case 'participant_joined':
          setParticipants(prev => [...prev, data.participant]);
          break;
        case 'participant_left':
          setParticipants(prev => 
            prev.filter(p => p.id !== data.participantId)
          );
          break;
        case 'analysis_update':
          // 실시간 분석 결과 업데이트
          break;
      }
    };
    
    return () => ws.close();
  }, [sessionId]);
  
  return { participants, isConnected };
}
```

---

## 📊 v2.0 구현 로드맵 및 우선순위

### 🚨 Priority 1: Critical Fixes (즉시 구현 필요)
- [ ] Missing core files 생성 (`design-tokens.ts`, `theme.ts`, `Stagger.tsx`)
- [ ] AppProvider 통합
- [ ] Button onClick 핸들러 구현
- [ ] TypeScript 타입 에러 수정

### ⚡ Priority 2: Core Functionality (1주 내)
- [ ] ControlsBar 저장/공유 기능 구현
- [ ] Service Tour 모달 구현
- [ ] Progressive Loading 시스템
- [ ] Error Boundary 강화

### ✨ Priority 3: UX Enhancement (2주 내)
- [ ] Session History 관리
- [ ] Accessibility 개선
- [ ] Performance 최적화
- [ ] 반응형 디자인 완성

### 🔮 Priority 4: Advanced Features (1개월 내)
- [ ] PWA 기능 추가
- [ ] 다국어 지원 (i18n)
- [ ] Real-time collaboration 기반 구축
- [ ] Analytics 및 사용자 피드백 수집

---

## 🧪 Testing Strategy v2.0

### Unit Testing
```typescript
// src/components/__tests__/SessionRecorder.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { SessionRecorder } from '../SessionRecorder';

describe('SessionRecorder v2.0', () => {
  const mockProps = {
    phase: 'idle' as const,
    duration: 0,
    currentRMS: 0,
    onStart: jest.fn(),
    onStop: jest.fn(),
  };

  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should render start button when idle', () => {
    render(<SessionRecorder {...mockProps} />);
    
    const button = screen.getByRole('button');
    expect(button).toBeInTheDocument();
    expect(button).toHaveTextContent('시작');
  });

  it('should call onStart when clicked', () => {
    render(<SessionRecorder {...mockProps} />);
    
    const button = screen.getByRole('button');
    fireEvent.click(button);
    
    expect(mockProps.onStart).toHaveBeenCalledTimes(1);
  });

  it('should be accessible via keyboard', () => {
    render(<SessionRecorder {...mockProps} />);
    
    const button = screen.getByRole('button');
    button.focus();
    
    expect(button).toHaveFocus();
    
    fireEvent.keyDown(button, { key: 'Enter' });
    expect(mockProps.onStart).toHaveBeenCalledTimes(1);
  });
});
```

### Integration Testing
```typescript
// src/__tests__/integration/analysis-flow.test.tsx
describe('Analysis Flow Integration', () => {
  it('should complete full analysis cycle', async () => {
    render(<App />);
    
    // 1. 시작 버튼 클릭
    const startButton = screen.getByText('지금 시작하기');
    fireEvent.click(startButton);
    
    // 2. 분석 섹션으로 스크롤 확인
    await waitFor(() => {
      expect(screen.getByText('실시간 대화 분석 체험')).toBeVisible();
    });
    
    // 3. 녹음 시작
    const recordButton = screen.getByRole('button', { name: /녹음/ });
    fireEvent.click(recordButton);
    
    // 4. 녹음 상태 확인
    expect(screen.getByText('녹음 중...')).toBeInTheDocument();
    
    // 5. 녹음 중지 및 분석 시작
    fireEvent.click(recordButton);
    
    // 6. 분석 결과 표시 확인
    await waitFor(() => {
      expect(screen.getByText(/매칭 점수/)).toBeInTheDocument();
    }, { timeout: 5000 });
  });
});
```

### E2E Testing
```typescript
// cypress/e2e/heartsignal-v2.cy.ts
describe('HeartSignal v2.0 E2E', () => {
  beforeEach(() => {
    cy.visit('/');
  });

  it('should complete user journey from landing to analysis', () => {
    // 1. 랜딩 페이지 로드 확인
    cy.contains('감정은 섬세하게, 결과는 간단하게');
    
    // 2. 서비스 투어 실행
    cy.contains('서비스 둘러보기').click();
    cy.get('[role="dialog"]').should('be.visible');
    cy.contains('다음').click();
    cy.contains('다음').click();
    cy.contains('완료').click();
    
    // 3. 분석 시작
    cy.contains('지금 시작하기').click();
    cy.get('#analysis-demo').should('be.visible');
    
    // 4. 녹음 및 분석
    cy.get('[data-testid="record-button"]').click();
    cy.wait(3000); // 3초간 녹음
    cy.get('[data-testid="record-button"]').click();
    
    // 5. 결과 확인
    cy.contains('매칭 점수', { timeout: 10000 });
    cy.get('[data-testid="score-gauge"]').should('be.visible');
    
    // 6. 결과 저장
    cy.contains('결과 저장').click();
    cy.contains('저장되었습니다');
  });
  
  it('should be accessible via keyboard navigation', () => {
    cy.get('body').tab();
    cy.focused().should('contain', '메인 콘텐츠로 건너뛰기');
    
    cy.focused().type('{enter}');
    cy.get('#main-content').should('have.focus');
    
    // Tab으로 모든 인터랙티브 요소 순회
    cy.get('button, a, input, select, textarea')
      .each(($el) => {
        cy.wrap($el).focus().should('have.focus');
      });
  });
});
```

---

## 📈 성능 지표 및 모니터링

### Core Web Vitals 목표
```typescript
// 성능 목표 설정
const PERFORMANCE_TARGETS = {
  LCP: 2.5, // Largest Contentful Paint < 2.5s
  FID: 100, // First Input Delay < 100ms  
  CLS: 0.1, // Cumulative Layout Shift < 0.1
  FCP: 1.8, // First Contentful Paint < 1.8s
  TTFB: 600, // Time to First Byte < 600ms
} as const;

// 실시간 모니터링
export function setupPerformanceMonitoring() {
  if (typeof window !== 'undefined') {
    import('web-vitals').then(({ getCLS, getFID, getFCP, getLCP, getTTFB }) => {
      getCLS((metric) => {
        console.log('CLS:', metric);
        if (metric.value > PERFORMANCE_TARGETS.CLS) {
          console.warn('CLS threshold exceeded:', metric.value);
        }
      });
      
      getFID((metric) => {
        console.log('FID:', metric);
        if (metric.value > PERFORMANCE_TARGETS.FID) {
          console.warn('FID threshold exceeded:', metric.value);
        }
      });
      
      // 기타 지표들...
    });
  }
}
```

### Bundle Size 최적화
```javascript
// next.config.ts - 번들 분석 설정
const nextConfig = {
  // Bundle analyzer
  webpack: (config, { buildId, dev, isServer, defaultLoaders, webpack }) => {
    if (process.env.ANALYZE) {
      const BundleAnalyzerPlugin = require('webpack-bundle-analyzer').BundleAnalyzerPlugin;
      config.plugins.push(
        new BundleAnalyzerPlugin({
          analyzerMode: 'server',
          analyzerPort: isServer ? 8888 : 8889,
          openAnalyzer: true,
        })
      );
    }
    
    return config;
  },
  
  // 이미지 최적화
  images: {
    formats: ['image/webp', 'image/avif'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
  },
  
  // 실험적 기능
  experimental: {
    optimizeCss: true,
    scrollRestoration: true,
  },
};
```

---

## 🎯 v2.0 성공 지표 (KPIs)

### 기술적 지표
- **코드 커버리지**: 80% 이상
- **TypeScript 엄격 모드**: 100% 적용
- **Lighthouse 점수**: 90점 이상 (모든 카테고리)
- **번들 크기**: 500KB 미만 (gzipped)

### 사용자 경험 지표
- **첫 화면 로딩 시간**: 2초 미만
- **분석 완료 시간**: 10초 미만
- **에러 발생률**: 1% 미만
- **접근성 점수**: WCAG 2.1 AA 준수

### 비즈니스 지표
- **사용자 만족도**: 4.5/5.0 이상
- **기능 완성도**: 100% (모든 버튼 동작)
- **크로스 브라우저 호환성**: 95% 이상
- **모바일 사용성**: 완벽 지원

---

## 🏁 결론: v2.0으로의 진화

HeartSignal v2.0은 v1.0의 견고한 아키텍처와 디자인 철학을 그대로 유지하면서, **실제 사용 가능한 완성도 높은 서비스**로 발전시키는 것이 목표입니다.

### 핵심 개선사항 요약

1. **✅ 완전한 기능 구현**: 모든 버튼과 로직이 실제로 동작
2. **🔧 견고한 인프라**: 누락된 핵심 파일들과 의존성 해결
3. **✨ 향상된 UX**: 프로그레시브 로딩, 에러 처리, 접근성
4. **⚡ 최적화된 성능**: Core Web Vitals 목표 달성
5. **🧪 완벽한 테스트**: 단위/통합/E2E 테스트 커버리지

### v1.0 대비 호환성

- **✅ 100% 하위 호환**: 기존 컴포넌트 인터페이스 유지
- **✅ 디자인 시스템**: v1.md의 모든 디자인 토큰 적용
- **✅ 아키텍처**: SOLID 원칙 기반 구조 그대로 유지
- **✅ 브랜드 아이덴티티**: "감정은 섬세하게, 결과는 간단하게" 철학 구현

v2.0은 단순한 업그레이드가 아닌, **실제 사용자에게 가치를 제공할 수 있는 완성된 서비스**로의 전환점입니다. 모든 개선사항은 사용자 중심적이며, 개발자 경험과 유지보수성을 동시에 고려했습니다.

---

## 🎭 Mock Data System v2.0 - 실제적인 테스트 환경

### 📊 새로운 Mock Data 기능

v2.0에서는 개발 및 데모 목적을 위한 완전히 새로운 Mock Data 시스템을 도입했습니다.

#### 🎯 주요 특징

**1. 현실적인 시나리오**
```typescript
// 대한민국 20대 남녀의 소개팅 후 첫 통화 시나리오
const scenario = {
  participants: "민수(25, 남) & 지영(24, 여)",
  situation: "지난주 소개팅 후 첫 통화",
  mood: "서로 탐색하는 단계, 약간의 긴장감과 호기심 공존",
  duration: "18분",
  outcome: "다음 주말 한강공원 데이트 약속 성사 ✨"
};
```

**2. 완전한 대화 전사 데이터**
- **39개 대화 세그먼트** (18분 분량)
- **자연스러운 대화 흐름**: 인사 → 취미 탐색 → 소개팅 회상 → 직장 이야기 → 농담과 케미 → 미래 계획 → 다음 약속
- **한국 문화 반영**: 존댓말, 소개팅 문화, 20대 관심사

**3. 실시간 음성 분석 데이터**
```typescript
// 감정 변화에 따른 prosody 패턴
const emotionalJourney = {
  initial: { rms: 0.3, pitch: 180 }, // 초반 긴장감
  warming: { rms: 0.6, pitch: 205 }, // 대화가 풀림
  chemistry: { rms: 0.7, pitch: 215 }, // 케미 발생
  comfortable: { rms: 0.58, pitch: 200 } // 안정적 마무리
};
```

### 🚀 새로운 페이지 전환 시스템

#### Before (v1.0)
```typescript
// ❌ 같은 페이지에서 데이터만 로드
const handleLoadMockData = () => {
  controller.loadMockData();
};
```

#### After (v2.0)
```typescript
// ✅ 전용 결과 페이지로 이동
const handleLoadMockData = () => {
  router.push('/mock-demo');
};
```

### 📱 개선된 결과 화면

#### 1. 시각적 개선
```typescript
// 메인 점수 표시
<div className="text-center bg-gradient-to-br from-pink-50 to-purple-50 rounded-2xl p-8">
  <div className={`text-7xl font-bold ${getScoreColor(match.score)} mb-2 animate-pulse`}>
    {match.score}
  </div>
  <p className="text-sm text-gray-500 mt-1">
    {match.score >= 80 && "🔥 완벽한 케미! 다음 만남이 기대돼요"}
    {match.score >= 60 && match.score < 80 && "💖 좋은 시작이에요! 더 깊이 알아가보세요"}
  </p>
</div>
```

#### 2. 인터랙티브 세부 점수
```typescript
// 애니메이션이 포함된 진행 바
<div className="w-full bg-gray-200 rounded-full h-2">
  <div
    className="bg-gradient-to-r from-blue-400 to-blue-600 h-2 rounded-full transition-all duration-1000"
    style={{ width: `${match.breakdown.text}%` }}
  />
</div>
```

#### 3. 자동 탭 전환
```typescript
// 데이터 로드 후 자동으로 결과 탭으로 이동
setTimeout(() => {
  setIsLoading(false);
  setActiveTab('score'); // 자동 포커스
}, 1500);
```

### 📋 Mock Data 구조

#### 완전한 데이터 세트
```typescript
export const mockSessionData = {
  // 39개 대화 세그먼트 (18분)
  segments: mockTranscriptSegments,
  
  // 시간별 음성 데이터
  prosody: mockProsodyData,
  
  // 감정 분석 결과
  emotion: {
    valence: 0.65,    // 전반적으로 긍정적
    arousal: 0.55,    // 적당한 흥미와 긴장감
    emotions: [
      { label: 'joy', score: 0.6 },
      { label: 'surprise', score: 0.25 },
      // ...
    ]
  },
  
  // 대화 품질 분석
  conversation: {
    rapport: 0.78,              // 좋은 라포 형성
    turnTakingBalance: 0.52,    // 자연스러운 균형
    empathy: 0.68,              // 서로에 대한 관심
    highlights: [
      '공통 관심사 발견',
      '자연스러운 유머',
      '미래 계획 공유'
    ]
  },
  
  // 매칭 점수 (v1.0 공식 적용)
  match: {
    score: 82,
    breakdown: {
      text: 85,    // 텍스트 분석
      voice: 78,   // 음성 분석  
      balance: 84  // 대화 균형
    }
  },
  
  // AI 피드백
  feedback: {
    summary: "첫 통화치고는 정말 자연스럽고 따뜻한 대화였어요!",
    tips: [
      "공감 표현을 더 자주 사용해보세요",
      "개인적인 경험을 구체적으로 공유해보세요",
      "감정을 묻는 질문을 해보세요"
    ]
  }
};
```

### 🎨 UX 개선사항

#### 1. 페이지 헤더 개선
```typescript
// Before: 단순한 제목
<h1>Mock 데이터 데모</h1>

// After: 브랜드 일관성 있는 헤더
<div className="inline-flex items-center space-x-2 bg-pink-100 px-4 py-2 rounded-full">
  <span className="text-2xl">💖</span>
  <span className="text-sm font-medium text-pink-700">
    실제 소개팅 시나리오 기반
  </span>
</div>
<h1 className="text-4xl md:text-5xl font-bold mb-4">
  HeartSignal 분석 결과
</h1>
```

#### 2. 반응형 레이아웃
- **모바일**: 단일 컬럼, 터치 친화적 인터페이스
- **태블릿**: 2컬럼 그리드, 적절한 간격
- **데스크톱**: 3컬럼 세부 점수, 풍부한 시각적 요소

#### 3. 접근성 개선
```typescript
// 스크린 리더를 위한 의미있는 레이블
<div role="progressbar" aria-valuenow={match.breakdown.text} aria-valuemax={100}>
  <span className="sr-only">텍스트 분석 점수: {match.breakdown.text}점</span>
</div>
```

### 🔧 개발자 경험 개선

#### 1. 다양한 로딩 옵션
```typescript
// 즉시 로드 (개발용)
loadMockSessionDataInstant(sessionStore);

// 시뮬레이션 로드 (데모용)
loadMockSessionData(sessionStore); // 1.5초 로딩 효과
```

#### 2. 모듈화된 데이터
```typescript
// 필요한 데이터만 개별 import 가능
import { 
  mockTranscriptSegments,
  mockEmotionAnalysis,
  mockMatchScore 
} from '@/lib/mock-data';
```

#### 3. TypeScript 완전 지원
- 모든 mock data는 기존 타입 시스템과 100% 호환
- 컴파일 타임 타입 검증
- IntelliSense 지원

### 📈 성능 최적화

#### 1. 지연 로딩
```typescript
// 대화 세그먼트 가상화
const VirtualizedTranscript = React.memo(({ segments }) => {
  // 보이는 영역의 메시지만 렌더링
});
```

#### 2. 메모이제이션
```typescript
const memoizedScoreColor = useMemo(() => 
  getScoreColor(match.score), [match.score]
);
```

#### 3. 애니메이션 최적화
```css
/* GPU 가속 활용 */
.animate-score-bar {
  will-change: width;
  transform: translateZ(0);
}
```

### 🎯 사용 시나리오

#### 1. 개발자 테스트
```typescript
// 컴포넌트 개발 시 즉시 데이터 확인
import { MockDataDemo } from '@/components/MockDataDemo';

function DevPage() {
  return <MockDataDemo />;
}
```

#### 2. 클라이언트 데모
```typescript
// 메인 페이지에서 "Mock 데이터 로드" 클릭
// → /mock-demo 페이지로 자동 이동
// → 실제적인 분석 결과 시연
```

#### 3. QA 테스트
```typescript
// 다양한 시나리오 테스트를 위한 확장 가능한 구조
export const mockScenarios = {
  successfulFirstCall: mockSessionData,
  awkwardConversation: mockSessionData2,
  perfectMatch: mockSessionData3
};
```

### 🌟 문화적 현실성

Mock data는 단순한 테스트 데이터가 아닌, **실제 한국의 소개팅 문화**를 반영합니다:

- **언어 패턴**: 존댓말, 겸손한 표현, 간접적 의사표현
- **관심사**: 드라마, 운동, 요리 등 20대가 공감할 수 있는 주제
- **감정 변화**: 초반 긴장 → 호기심 → 편안함 → 기대감의 자연스러운 흐름
- **결과**: 현실적인 다음 만남 약속으로 마무리

이러한 디테일은 HeartSignal이 단순한 기술 데모가 아닌, **실제 사용자의 니즈를 이해하는 서비스**임을 보여줍니다.

---

*"좋은 소프트웨어는 사용자가 의식하지 못할 때 완성된다."* - HeartSignal v2.0 개발 철학

