# HeartSignal v5.0 개발 계획

## 📋 v5.0 개발 개요

**개발 목표**: 사용자 참여도 향상을 위한 인터랙티브 기능 강화  
**개발 기간**: 1-2주  
**우선순위**: High Priority  

---

## 🎯 v5.0 주요 기능

### 1. 스크롤 네비게이션 시스템 🔥

#### **기능 개요**
메인 페이지의 '지금 시작하기' 버튼 클릭 시 자동으로 '특별한 기능들' 섹션으로 부드럽게 스크롤 이동하는 기능을 구현합니다.

#### **구현 상세**

**1.1 스크롤 타겟 설정**
```typescript
// 현재 구조 분석
// src/app/page.tsx:196-268 - Features Section (특별한 기능들)
// 이 섹션을 스크롤 타겟으로 설정

// 구현 방법
<section 
  id="features" // 스크롤 타겟 ID 추가
  className="py-16 md:py-24 bg-neutral-50 dark:bg-neutral-900/50"
>
```

**1.2 스크롤 함수 구현**
```typescript
// 부드러운 스크롤 함수 구현
const scrollToFeatures = () => {
  const featuresSection = document.getElementById('features');
  if (featuresSection) {
    featuresSection.scrollIntoView({
      behavior: 'smooth',
      block: 'start',
      inline: 'nearest'
    });
  }
};
```

**1.3 버튼 이벤트 연결**
```typescript
// 현재 위치: src/app/page.tsx:77-80
<Button 
  size="lg" 
  className="bg-primary-500 hover:bg-primary-600 text-white px-8 py-3"
  onClick={scrollToFeatures} // 스크롤 함수 연결
>
  <Play className="h-5 w-5 mr-2" />
  지금 시작하기
</Button>
```

### 2. 구독하기 버튼 시스템 📱

#### **기능 개요**
'특별한 기능들' 섹션 하단에 '구독하기' 버튼을 추가하여 사용자의 관심을 실제 서비스 구독으로 연결합니다.

#### **구현 상세**

**2.1 구독 섹션 추가**
```typescript
// Features Section 직후에 새로운 CTA 섹션 추가
// 위치: src/app/page.tsx:268 (Features Section 종료 후)

<section className="py-12 md:py-16 bg-gradient-to-r from-primary-50 to-secondary-50 dark:from-primary-900/30 dark:to-secondary-900/30">
  <Container>
    <div className="text-center space-y-6">
      <h3 className="text-2xl md:text-3xl font-bold text-neutral-900 dark:text-neutral-100">
        더 나은 소개팅을 시작해보세요
      </h3>
      <p className="text-lg text-neutral-600 dark:text-neutral-400 max-w-2xl mx-auto">
        HeartSignal과 함께 성공적인 만남을 경험하고, 
        당신만의 연애 스토리를 만들어보세요.
      </p>
      <div className="flex flex-col sm:flex-row items-center justify-center gap-4">
        <Button 
          size="lg" 
          className="bg-primary-500 hover:bg-primary-600 text-white px-8 py-3"
          onClick={handleSubscribe}
        >
          <Heart className="h-5 w-5 mr-2" />
          구독하기
        </Button>
        <Button 
          variant="outline" 
          size="lg" 
          className="px-8 py-3"
          onClick={() => router.push('/mypage')}
        >
          마이페이지 보기
        </Button>
      </div>
    </div>
  </Container>
</section>
```

**2.2 구독 처리 함수**
```typescript
// 구독 버튼 클릭 처리 함수
const handleSubscribe = () => {
  // 현재는 알림으로 처리, 추후 실제 구독 시스템 연동 가능
  alert('구독 기능은 곧 출시될 예정입니다! 🎉');
  
  // 또는 토스트 알림 시스템이 구현되어 있다면
  // toast.success('구독 기능은 곧 출시될 예정입니다! 🎉');
  
  // 추후 확장 가능성
  // - 이메일 수집 모달
  // - 외부 구독 서비스 연동
  // - 결제 시스템 연동
};
```

### 3. 사용자 경험 개선 ✨

#### **3.1 스크롤 인디케이터**
```typescript
// 스크롤 진행 상태를 시각적으로 표시
const [scrollProgress, setScrollProgress] = useState(0);

useEffect(() => {
  const handleScroll = () => {
    const totalScroll = document.documentElement.scrollTop;
    const windowHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
    const scroll = `${totalScroll / windowHeight}`;
    setScrollProgress(scroll);
  };

  window.addEventListener('scroll', handleScroll);
  return () => window.removeEventListener('scroll', handleScroll);
}, []);
```

#### **3.2 애니메이션 효과**
```typescript
// 버튼 클릭 시 시각적 피드백
const [isScrolling, setIsScrolling] = useState(false);

const scrollToFeatures = () => {
  setIsScrolling(true);
  
  const featuresSection = document.getElementById('features');
  if (featuresSection) {
    featuresSection.scrollIntoView({
      behavior: 'smooth',
      block: 'start',
    });
    
    // 스크롤 완료 후 상태 리셋
    setTimeout(() => setIsScrolling(false), 1000);
  }
};
```

---

## 🏗️ 구현 우선순위

### Phase 5.1: 핵심 스크롤 기능 (High Priority)

1. **스크롤 타겟 설정** 🔥
   - Features Section에 `id="features"` 추가
   - 스크롤 위치 최적화

2. **스크롤 함수 구현** 🔥
   - `scrollToFeatures()` 함수 작성
   - 부드러운 스크롤 애니메이션 적용

3. **버튼 이벤트 연결** 🔥
   - '지금 시작하기' 버튼에 onClick 이벤트 추가
   - 클릭 피드백 구현

### Phase 5.2: 구독 시스템 (Medium Priority)

4. **구독 섹션 UI 구현** 📱
   - CTA 섹션 추가
   - 구독하기 버튼 스타일링

5. **구독 처리 로직** 📱
   - `handleSubscribe()` 함수 구현
   - 임시 알림 시스템 (추후 확장 가능)

6. **추가 CTA 버튼** 📱
   - 마이페이지 연결 버튼
   - 버튼 그룹 레이아웃

### Phase 5.3: 사용자 경험 향상 (Low Priority)

7. **스크롤 인디케이터** ✨
   - 스크롤 진행률 표시
   - 시각적 진행 상태

8. **애니메이션 효과** ✨
   - 버튼 클릭 애니메이션
   - 스크롤 중 로딩 상태

9. **접근성 개선** ♿
   - 키보드 네비게이션 지원
   - ARIA 라벨 추가

---

## 📊 기술적 고려사항

### 성능 최적화
```typescript
// 스크롤 이벤트 디바운싱
import { debounce } from 'es-toolkit';

const debouncedScrollHandler = debounce((event) => {
  // 스크롤 처리 로직
}, 16); // 60fps 기준
```

### 타입 안전성
```typescript
// 스크롤 관련 타입 정의
interface ScrollOptions {
  target: string;
  behavior: ScrollBehavior;
  block: ScrollLogicalPosition;
  offset?: number;
}

interface ScrollState {
  isScrolling: boolean;
  progress: number;
  currentSection: string;
}
```

### 접근성 준수
```typescript
// ARIA 라벨 및 역할 정의
<Button
  onClick={scrollToFeatures}
  aria-label="특별한 기능 섹션으로 이동"
  role="button"
  tabIndex={0}
>
  지금 시작하기
</Button>
```

---

## 🎯 v5.0 성공 지표

### 기능 완성도
- [ ] 스크롤 네비게이션: 100% 구현
- [ ] 구독하기 버튼: 100% 구현  
- [ ] 사용자 피드백: 시각적 인디케이터 구현
- [ ] 접근성 준수: WCAG 2.1 AA 기준

### 사용자 경험
- [ ] 스크롤 애니메이션 부드러움 (60fps 유지)
- [ ] 버튼 반응성 향상 (클릭 피드백)
- [ ] 모바일 최적화 (터치 인터랙션)
- [ ] 키보드 네비게이션 지원

### 코드 품질
- [ ] TypeScript strict 모드 100% 준수
- [ ] 컴포넌트 재사용성 확보
- [ ] 성능 최적화 (디바운싱 등)
- [ ] 에러 처리 및 예외 상황 대응

---

## 🚀 향후 확장 가능성

### 고급 스크롤 기능
- 스크롤 스파이 (현재 섹션 하이라이트)
- 섹션별 네비게이션 메뉴
- 스크롤 기반 애니메이션 (Intersection Observer)

### 구독 시스템 고도화
- 이메일 수집 모달
- 구독 플랜 선택 UI
- 결제 시스템 연동 (Stripe, Toss Payments)
- 구독자 전용 콘텐츠 제공

### 분석 및 추적
- 스크롤 행동 분석 (Google Analytics)
- 버튼 클릭률 추적
- A/B 테스트 시스템 구축

---

## 📝 구현 체크리스트

### 필수 구현 항목
- [ ] `src/app/page.tsx`에 스크롤 함수 추가
- [ ] Features Section에 ID 속성 추가
- [ ] '지금 시작하기' 버튼에 onClick 이벤트 연결
- [ ] 구독 섹션 UI 구현
- [ ] 구독하기 버튼 이벤트 처리
- [ ] 반응형 디자인 검증
- [ ] 접근성 테스트

### 선택적 개선 항목
- [ ] 스크롤 진행률 인디케이터
- [ ] 버튼 클릭 애니메이션
- [ ] 스크롤 디바운싱
- [ ] 에러 바운더리 추가
- [ ] 단위 테스트 작성

---

**문서 작성일**: 2024년 12월 22일  
**예상 완료일**: 2024년 12월 29일  
**담당 개발자**: HeartSignal 개발팀  
**검토자**: 시니어 개발자