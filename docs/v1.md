# HeartSignal v1.0 - 아키텍처 & 디자인 시스템 💖

> **"감정은 섬세하게, 결과는 간단하게"** - HeartSignal의 핵심 철학

## 🏗️ 아키텍처 설계 원칙

### SOLID 원칙 적용

#### 1. Single Responsibility Principle (단일 책임 원칙)
- **컴포넌트**: 각 컴포넌트는 하나의 명확한 책임만 가짐
- **훅**: 특정 도메인 로직만 처리
- **유틸리티**: 단일 기능만 수행

#### 2. Open/Closed Principle (개방/폐쇄 원칙)
- **확장 가능한 컴포넌트**: props interface를 통한 확장
- **테마 시스템**: 새로운 테마 추가 시 기존 코드 수정 없음
- **애니메이션 시스템**: 새로운 애니메이션 추가 용이

#### 3. Liskov Substitution Principle (리스코프 치환 원칙)
- **인터페이스 일관성**: 동일한 interface를 구현하는 컴포넌트는 교체 가능
- **어댑터 패턴**: STT, LLM 어댑터들은 동일한 인터페이스 구현

#### 4. Interface Segregation Principle (인터페이스 분리 원칙)
- **작은 인터페이스**: 필요한 메서드만 포함
- **선택적 props**: 불필요한 의존성 제거

#### 5. Dependency Inversion Principle (의존성 역전 원칙)
- **의존성 주입**: 구체적 구현이 아닌 추상화에 의존
- **컨텍스트 활용**: 상위 레벨에서 의존성 관리

## 📐 레이아웃 구조

### 전체 앱 레이아웃
```
┌─────────────────────────────────┐
│           Header                │ ← 고정 높이 (64px)
├─────────────────────────────────┤
│                                 │
│           Main Content          │ ← 가변 높이
│                                 │
├─────────────────────────────────┤
│           Footer                │ ← 고정 높이 (80px)
└─────────────────────────────────┘
```

### Header 구성 요소
```typescript
interface HeaderProps {
  title?: string;
  showLogo?: boolean;
  showNavigation?: boolean;
  actions?: React.ReactNode;
  className?: string;
}
```

### Footer 구성 요소  
```typescript
interface FooterProps {
  showBranding?: boolean;
  showLinks?: boolean;
  showSocial?: boolean;
  className?: string;
}
```

## 🎨 디자인 시스템

### 브랜드 컬러 팔레트
```css
/* Primary Colors */
--heart-pink: #E95877        /* 메인 브랜드 컬러 */
--heart-pink-light: #F26B88  /* 호버 상태 */
--heart-pink-50: #FDF2F8     /* 배경 */

/* Secondary Colors */  
--purple-primary: #7B6EF6    /* 액센트 컬러 */
--purple-50: #F3F0FF         /* 배경 */

/* Functional Colors */
--success: #2FBF71           /* 성공/긍정 */
--warning: #F59E0B           /* 주의/중간 */
--danger: #E11D48            /* 위험/부정 */
--info: #3B82F6              /* 정보 */

/* Neutral Colors */
--gray-900: #111827          /* 메인 텍스트 */
--gray-700: #374151          /* 서브 텍스트 */
--gray-500: #6B7280          /* 보조 텍스트 */
--gray-200: #E5E7EB          /* 구분선 */
--gray-50: #F9FAFB           /* 배경 */
```

### 타이포그래피
```css
/* 폰트 패밀리 */
font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;

/* 폰트 크기 스케일 */
--text-4xl: 2.25rem;    /* 36px - 메인 제목 */
--text-2xl: 1.5rem;     /* 24px - 섹션 제목 */
--text-lg: 1.125rem;    /* 18px - 강조 텍스트 */
--text-base: 1rem;      /* 16px - 본문 */
--text-sm: 0.875rem;    /* 14px - 보조 텍스트 */
--text-xs: 0.75rem;     /* 12px - 캡션 */

/* 폰트 가중치 */
--font-bold: 700;       /* 제목, 강조 */
--font-semibold: 600;   /* 서브 제목 */
--font-medium: 500;     /* 버튼, 라벨 */
--font-normal: 400;     /* 본문 */
```

### 간격 시스템 (8px 기준)
```css
--space-1: 0.25rem;     /* 4px */
--space-2: 0.5rem;      /* 8px */
--space-3: 0.75rem;     /* 12px */
--space-4: 1rem;        /* 16px */
--space-6: 1.5rem;      /* 24px */
--space-8: 2rem;        /* 32px */
--space-12: 3rem;       /* 48px */
--space-16: 4rem;       /* 64px */
```

### 그림자 & 깊이
```css
/* 그림자 레벨 */
--shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
--shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
--shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
--shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1);

/* 컬러 그림자 */
--shadow-pink: 0 10px 15px -3px rgb(233 88 119 / 0.2);
--shadow-purple: 0 10px 15px -3px rgb(123 110 246 / 0.2);
```

## 🎭 애니메이션 시스템

### 애니메이션 원칙
1. **의미 있는 모션**: 모든 애니메이션은 사용자 경험을 향상시키는 목적
2. **일관된 타이밍**: 브랜드 개성을 반영하는 부드럽고 따뜻한 움직임
3. **성능 우선**: GPU 가속 속성만 사용 (transform, opacity)
4. **접근성 고려**: `prefers-reduced-motion` 미디어 쿼리 지원

### 타이밍 함수
```css
/* Easing Functions */
--ease-out: cubic-bezier(0.0, 0.0, 0.2, 1);      /* 빠른 시작, 부드러운 끝 */
--ease-in: cubic-bezier(0.4, 0.0, 1, 1);         /* 부드러운 시작, 빠른 끝 */
--ease-in-out: cubic-bezier(0.4, 0.0, 0.2, 1);   /* 부드러운 시작과 끝 */
--ease-spring: cubic-bezier(0.68, -0.55, 0.265, 1.55); /* 스프링 효과 */
```

### 지속 시간
```css
--duration-fast: 150ms;     /* 호버, 포커스 */
--duration-normal: 300ms;   /* 일반적인 전환 */
--duration-slow: 500ms;     /* 복잡한 애니메이션 */
--duration-slower: 800ms;   /* 강조 애니메이션 */
```

## 🔧 컴포넌트 아키텍처

### 컴포넌트 계층 구조
```
App Layout
├── Header
│   ├── Logo
│   ├── Navigation
│   └── UserActions
├── Main Content
│   ├── SessionContainer
│   │   ├── SessionRecorder
│   │   ├── LiveTranscriptPanel
│   │   └── ControlsBar
│   ├── AnalysisContainer
│   │   ├── ScoreGauge
│   │   └── FeedbackBubble
│   └── ErrorBoundary
└── Footer
    ├── Branding
    ├── Links
    └── SocialMedia
```

### 재사용 가능한 기본 컴포넌트

#### 1. Layout Components
```typescript
// src/components/layout/Header.tsx
interface HeaderProps {
  title?: string;
  showLogo?: boolean;
  showNavigation?: boolean;
  actions?: React.ReactNode;
  className?: string;
}

// src/components/layout/Footer.tsx
interface FooterProps {
  showBranding?: boolean;
  showLinks?: boolean;
  showSocial?: boolean;
  className?: string;
}

// src/components/layout/Container.tsx
interface ContainerProps {
  size?: 'sm' | 'md' | 'lg' | 'xl' | 'full';
  padding?: boolean;
  className?: string;
  children: React.ReactNode;
}
```

#### 2. Base UI Components
```typescript
// src/components/ui/Button.tsx - 이미 존재, 확장 가능
interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'ghost' | 'danger';
  size?: 'sm' | 'md' | 'lg' | 'xl';
  loading?: boolean;
  icon?: React.ReactNode;
  iconPosition?: 'left' | 'right';
}

// src/components/ui/Card.tsx
interface CardProps {
  variant?: 'default' | 'elevated' | 'outlined' | 'ghost';
  padding?: 'none' | 'sm' | 'md' | 'lg';
  className?: string;
  children: React.ReactNode;
}

// src/components/ui/Badge.tsx
interface BadgeProps {
  variant?: 'default' | 'success' | 'warning' | 'danger' | 'info';
  size?: 'sm' | 'md' | 'lg';
  className?: string;
  children: React.ReactNode;
}
```

#### 3. Animation Components
```typescript
// src/components/animation/FadeIn.tsx
interface FadeInProps {
  delay?: number;
  duration?: number;
  direction?: 'up' | 'down' | 'left' | 'right';
  className?: string;
  children: React.ReactNode;
}

// src/components/animation/Stagger.tsx
interface StaggerProps {
  staggerDelay?: number;
  className?: string;
  children: React.ReactNode[];
}

// src/components/animation/Pulse.tsx
interface PulseProps {
  intensity?: 'subtle' | 'medium' | 'strong';
  speed?: 'slow' | 'normal' | 'fast';
  className?: string;
  children: React.ReactNode;
}
```

### 컴포넌트 조합 패턴

#### Compound Component Pattern
```typescript
// src/components/ui/Modal.tsx
const Modal = {
  Root: ModalRoot,
  Trigger: ModalTrigger,
  Content: ModalContent,
  Header: ModalHeader,
  Body: ModalBody,
  Footer: ModalFooter,
  Close: ModalClose,
};

// 사용 예시
<Modal.Root>
  <Modal.Trigger>Open Modal</Modal.Trigger>
  <Modal.Content>
    <Modal.Header>Title</Modal.Header>
    <Modal.Body>Content</Modal.Body>
    <Modal.Footer>Actions</Modal.Footer>
  </Modal.Content>
</Modal.Root>
```

#### Render Props Pattern
```typescript
// src/components/animation/AnimateOnView.tsx
interface AnimateOnViewProps {
  children: (inView: boolean, ref: RefObject<HTMLElement>) => React.ReactNode;
  threshold?: number;
  triggerOnce?: boolean;
}
```

## 🎨 디자인 토큰 시스템

### 토큰 구조
```typescript
// src/lib/design-tokens.ts
export const designTokens = {
  colors: {
    primary: {
      50: '#FDF2F8',
      100: '#FCE7F3', 
      500: '#E95877',  // Main brand color
      600: '#D946A0',
      900: '#831843',
    },
    semantic: {
      success: '#2FBF71',
      warning: '#F59E0B', 
      danger: '#E11D48',
      info: '#3B82F6',
    }
  },
  spacing: {
    xs: '0.25rem',   // 4px
    sm: '0.5rem',    // 8px
    md: '1rem',      // 16px
    lg: '1.5rem',    // 24px
    xl: '2rem',      // 32px
    '2xl': '3rem',   // 48px
  },
  typography: {
    fontFamily: {
      sans: ['Pretendard', 'system-ui', 'sans-serif'],
      mono: ['JetBrains Mono', 'monospace'],
    },
    fontSize: {
      xs: '0.75rem',
      sm: '0.875rem', 
      base: '1rem',
      lg: '1.125rem',
      xl: '1.25rem',
      '2xl': '1.5rem',
      '4xl': '2.25rem',
    }
  },
  animation: {
    duration: {
      fast: '150ms',
      normal: '300ms', 
      slow: '500ms',
      slower: '800ms',
    },
    easing: {
      easeOut: 'cubic-bezier(0.0, 0.0, 0.2, 1)',
      easeIn: 'cubic-bezier(0.4, 0.0, 1, 1)',
      easeInOut: 'cubic-bezier(0.4, 0.0, 0.2, 1)',
      spring: 'cubic-bezier(0.68, -0.55, 0.265, 1.55)',
    }
  }
} as const;
```

### 테마 시스템
```typescript
// src/lib/theme.ts
interface Theme {
  colors: typeof designTokens.colors;
  spacing: typeof designTokens.spacing;
  typography: typeof designTokens.typography;
  animation: typeof designTokens.animation;
}

export const lightTheme: Theme = {
  ...designTokens,
  // Light theme specific overrides
};

export const darkTheme: Theme = {
  ...designTokens,
  colors: {
    ...designTokens.colors,
    // Dark theme color overrides
  }
};
```

## 🧩 핵심 컴포넌트 디자인

### 1. SessionRecorder (메인 녹음 버튼)

#### 시각적 특징
- **크기**: 128x128px (8rem)
- **모양**: 완전한 원형
- **그라데이션**: 미묘한 내부 그림자로 입체감
- **상태별 컬러**:
  - 대기: `#E95877` (HeartSignal 핑크)
  - 녹음: `#EF4444` (빨간색)
  - 처리중: 회전 애니메이션과 함께

#### 애니메이션 세부사항
```typescript
// 녹음 중 펄스 애니메이션
const pulseAnimation = {
  scale: [1, 1.1, 1],
  opacity: [1, 0.8, 1],
  transition: {
    duration: 2,
    repeat: Infinity,
    ease: "easeInOut"
  }
}

// 외부 링 애니메이션
const ringAnimation = {
  scale: [1, 1.5, 1],
  opacity: [0.7, 0, 0.7],
  transition: {
    duration: 2,
    repeat: Infinity,
    ease: "easeOut"
  }
}
```

#### 상태별 피드백
- **호버**: 10% 크기 증가, 그림자 강화
- **클릭**: 스케일 다운 후 업 (0.95 → 1.05 → 1.0)
- **녹음 시작**: 색상 전환 + 펄스 시작
- **처리 중**: 아이콘 회전 + 반투명

### 2. LiveTranscriptPanel (실시간 전사 패널)

#### 레이아웃 구조
```
┌─────────────────────────────────┐
│  [Avatar] 이름    시간          │
│  대화 내용이 여기에 표시됩니다  │
│  ↳ 말풍선 꼬리                  │
└─────────────────────────────────┘
```

#### 말풍선 디자인
- **화자별 정렬**: 나(왼쪽), 상대방(오른쪽)
- **컬러 구분**: 
  - 나: `bg-pink-50`, `border-pink-100`
  - 상대방: `bg-gray-100`, `border-gray-200`
- **둥근 모서리**: `rounded-2xl` (16px)
- **꼬리 위치**: 말풍선 상단 모서리

#### 애니메이션
```typescript
// 새 메시지 등장
const messageEnter = {
  initial: { opacity: 0, y: 10 },
  animate: { opacity: 1, y: 0 },
  transition: { duration: 0.3, ease: "easeOut" }
}

// 타이핑 인디케이터
const typingDots = {
  animate: { scale: [1, 1.2, 1] },
  transition: { 
    duration: 1, 
    repeat: Infinity,
    delay: staggered // 0, 0.2, 0.4초
  }
}
```

### 3. ScoreGauge (점수 게이지)

#### 시각적 구조
- **형태**: 반원형 호 (180도)
- **크기**: 200x120px
- **두께**: 12px
- **배경**: `#E5E7EB` (연한 회색)
- **진행**: 점수에 따른 컬러 그라데이션

#### 점수별 컬러 시스템
```typescript
const getScoreColor = (score: number) => {
  if (score >= 80) return '#2FBF71';  // 성공 (초록)
  if (score >= 60) return '#E95877';  // 양호 (핑크)
  if (score >= 40) return '#F59E0B';  // 주의 (노랑)
  return '#E11D48';                   // 개선 필요 (빨강)
}
```

#### 애니메이션 시퀀스
1. **게이지 채우기** (1.5초): `strokeDashoffset` 애니메이션
2. **점수 등장** (0.6초): 스케일 + 페이드인, 스프링 효과
3. **해석 텍스트** (0.3초): 페이드인

### 4. FeedbackBubble (AI 피드백)

#### 디자인 특징
- **배경**: 그라데이션 `from-pink-50 to-purple-50`
- **테두리**: `border-pink-100`
- **모서리**: `rounded-3xl` (24px)
- **그림자**: 미묘한 드롭 섀도우
- **아이콘**: 하트 모양, 브랜드 컬러

#### 타이핑 애니메이션
```typescript
// 문자별 타이핑 효과
const typingEffect = {
  interval: 30, // 30ms마다 한 글자
  cursor: true, // 커서 깜빡임
  onComplete: () => startTipAnimation()
}

// 팁 순차 등장
const tipStagger = {
  initial: { opacity: 0, x: -20 },
  animate: { opacity: 1, x: 0 },
  transition: { 
    duration: 0.4,
    delay: index * 0.2, // 200ms씩 지연
    ease: "easeOut"
  }
}
```

## 📱 반응형 디자인

### 브레이크포인트
```css
/* Mobile First 접근법 */
--mobile: 320px;    /* 최소 너비 */
--tablet: 768px;    /* 태블릿 */
--desktop: 1024px;  /* 데스크톱 */
--wide: 1280px;     /* 와이드 스크린 */
```

### 모바일 최적화 (320px - 767px)
- **SessionRecorder**: 크기 축소 (96x96px)
- **LiveTranscriptPanel**: 높이 조정 (240px)
- **ScoreGauge**: 크기 축소 (160x96px)
- **FeedbackBubble**: 패딩 축소, 폰트 크기 조정

### 태블릿 최적화 (768px - 1023px)
- **레이아웃**: 2컬럼 그리드 활용
- **터치 타겟**: 최소 44x44px 보장
- **간격**: 모바일보다 여유롭게

### 데스크톱 최적화 (1024px+)
- **레이아웃**: 3컬럼 또는 사이드바 활용
- **호버 효과**: 풍부한 인터랙션
- **키보드 내비게이션**: 포커스 링 강화

## 🎪 페이지 전환 & 상태 애니메이션

### 페이지 진입 애니메이션
```typescript
const pageEnter = {
  initial: { opacity: 0, y: 20 },
  animate: { opacity: 1, y: 0 },
  exit: { opacity: 0, y: -20 },
  transition: { duration: 0.3, ease: "easeOut" }
}
```

### 로딩 상태
```typescript
// 스켈레톤 로더
const skeletonPulse = {
  animate: { opacity: [0.5, 1, 0.5] },
  transition: { duration: 1.5, repeat: Infinity }
}

// 스피너 (처리 중)
const spinnerRotate = {
  animate: { rotate: 360 },
  transition: { duration: 2, repeat: Infinity, ease: "linear" }
}
```

### 에러 상태
- **컬러**: `--danger` 빨간색 계열
- **아이콘**: 경고 삼각형 또는 X 표시
- **애니메이션**: 부드러운 흔들림 효과

## 🔊 오디오 시각화

### 실시간 레벨미터
```typescript
// 6개 바로 구성된 레벨미터
const levelBars = Array.from({ length: 6 }, (_, i) => ({
  height: 12 + i * 4, // 12px부터 32px까지
  threshold: (i + 1) / 6, // 0.17, 0.33, 0.5, 0.67, 0.83, 1.0
  color: currentRMS > threshold ? '#7B6EF6' : '#E5E7EB'
}))

// 바 애니메이션
const barAnimation = {
  animate: { scaleY: [1, 1.2, 1] },
  transition: { duration: 0.2, ease: "easeOut" }
}
```

## 🎯 마이크로 인터랙션

### 버튼 피드백
1. **호버**: 크기 105%, 그림자 강화
2. **포커스**: 아웃라인 링 표시
3. **클릭**: 스케일 95% → 105% → 100%
4. **비활성**: 50% 투명도, 회색톤

### 폼 요소
- **입력 포커스**: 테두리 컬러 변경 + 그림자
- **유효성 검사**: 실시간 컬러 피드백
- **자동완성**: 부드러운 드롭다운 애니메이션

### 툴팁 & 모달
- **등장**: 스케일 + 페이드인
- **위치**: 스마트 포지셔닝 (화면 경계 감지)
- **배경**: 블러 처리된 오버레이

## 🚀 성능 최적화

### 애니메이션 최적화
```css
/* GPU 가속 활성화 */
.animate-element {
  will-change: transform, opacity;
  transform: translateZ(0);
}

/* 리페인트 최소화 */
.smooth-transition {
  transition: transform 0.3s ease-out,
              opacity 0.3s ease-out;
}
```

### 이미지 & 미디어
- **지연 로딩**: Intersection Observer 활용
- **최적화된 포맷**: WebP, AVIF 지원
- **반응형 이미지**: `srcset` 및 `sizes` 속성

### 번들 크기 최적화
- **Framer Motion**: 필요한 컴포넌트만 import
- **Lucide Icons**: 개별 아이콘 import
- **CSS**: 사용하지 않는 스타일 제거

## 🎨 브랜드 일관성

### 시각적 언어
- **둥근 모서리**: 따뜻하고 친근한 느낌
- **부드러운 그림자**: 깊이감과 고급스러움
- **그라데이션**: 미묘한 컬러 전환으로 생동감
- **여백**: 충분한 화이트스페이스로 시각적 휴식

### 톤 & 매너
- **따뜻함**: 핑크/퍼플 계열의 온화한 컬러
- **신뢰감**: 일관된 타이포그래피와 레이아웃
- **현대적**: 미니멀하면서도 기능적인 디자인
- **감성적**: 하트 모티프와 부드러운 애니메이션

## 🔄 상태 관리 아키텍처

### Zustand Store 구조 (SOLID 원칙 적용)
```typescript
// src/store/session/types.ts - Interface Segregation
export interface SessionState {
  phase: SessionPhase;
  duration: number;
  currentRMS: number;
  // ... 세션 관련 상태만
}

export interface AudioState {
  audioBlob: Blob | null;
  prosody: ProsodySample[];
  // ... 오디오 관련 상태만
}

export interface AnalysisState {
  emotion: EmotionAnalysis | null;
  conversation: ConversationAnalysis | null;
  match: MatchScore | null;
  feedback: Feedback | null;
  // ... 분석 관련 상태만
}

// src/store/session/actions.ts - Single Responsibility
export interface SessionActions {
  reset(): void;
  setPhase(phase: SessionPhase): void;
  setDuration(duration: number): void;
}

export interface AudioActions {
  setAudioBlob(blob: Blob): void;
  pushProsody(sample: ProsodySample): void;
  setCurrentRMS(rms: number): void;
}

export interface AnalysisActions {
  setEmotion(analysis: EmotionAnalysis): void;
  setConversation(analysis: ConversationAnalysis): void;
  setMatch(score: MatchScore): void;
  setFeedback(feedback: Feedback): void;
}
```

### 컨텍스트 기반 의존성 주입
```typescript
// src/contexts/AppContext.tsx - Dependency Inversion
interface AppContextValue {
  theme: Theme;
  setTheme: (theme: Theme) => void;
  sttAdapter: STTAdapter;
  llmAdapter: LLMAdapter;
}

// src/contexts/SessionContext.tsx
interface SessionContextValue {
  controller: AnalysisController;
  recorder: AudioRecorder;
}
```

## 🧪 테스트 전략

### 단위 테스트 (Component Level)
```typescript
// src/components/__tests__/SessionRecorder.test.tsx
describe('SessionRecorder', () => {
  it('should handle phase transitions correctly', () => {
    // Single Responsibility 테스트
  });
  
  it('should be substitutable with different recorders', () => {
    // Liskov Substitution 테스트
  });
});
```

### 통합 테스트 (Feature Level)
```typescript
// src/features/__tests__/session-flow.test.tsx
describe('Session Flow', () => {
  it('should complete full analysis cycle', () => {
    // 전체 플로우 테스트
  });
});
```

### E2E 테스트
```typescript
// cypress/e2e/heartsignal.cy.ts
describe('HeartSignal App', () => {
  it('should record and analyze conversation', () => {
    // 사용자 시나리오 테스트
  });
});
```

## 📋 구현 로드맵

### Phase 1: 기반 구조 (진행 중 🔄)
- [x] SOLID 원칙 기반 아키텍처 설계
- [x] 디자인 토큰 시스템 구축
- [x] 재사용 가능한 기본 컴포넌트 정의
- [ ] Header/Footer 레이아웃 컴포넌트 구현
- [ ] 테마 시스템 구현
- [ ] 애니메이션 컴포넌트 라이브러리 구축

### Phase 2: 핵심 기능 (완료 ✅)
- [x] SessionRecorder 컴포넌트
- [x] LiveTranscriptPanel 컴포넌트  
- [x] ScoreGauge 컴포넌트
- [x] FeedbackBubble 컴포넌트
- [x] 상태 관리 시스템
- [x] 도메인 로직 구현

### Phase 3: 통합 & 최적화 (대기 중 ⏳)
- [ ] 메인 앱 레이아웃 통합
- [ ] 컴포넌트 간 데이터 흐름 최적화
- [ ] 에러 바운더리 구현
- [ ] 로딩 상태 관리
- [ ] 성능 최적화

### Phase 4: 사용자 경험 (대기 중 ⏳)
- [ ] 반응형 디자인 구현
- [ ] 접근성 개선
- [ ] 마이크로 인터랙션 추가
- [ ] 다크 모드 지원
- [ ] PWA 기능 추가

### Phase 5: 품질 보증 (대기 중 ⏳)
- [ ] 단위 테스트 작성
- [ ] 통합 테스트 작성
- [ ] E2E 테스트 작성
- [ ] 성능 테스트
- [ ] 접근성 테스트

## 🎯 코드 품질 가이드라인

### ESLint & TypeScript 규칙
```json
// .eslintrc.json 추가 규칙
{
  "rules": {
    "@typescript-eslint/explicit-function-return-type": "warn",
    "@typescript-eslint/no-unused-vars": "error",
    "react/prop-types": "off",
    "react/display-name": "error",
    "react-hooks/exhaustive-deps": "warn"
  }
}
```

### 파일 구조 규칙
```
src/
├── components/
│   ├── layout/           # Header, Footer, Container 등
│   ├── ui/              # 기본 UI 컴포넌트 (shadcn-ui 확장)
│   ├── animation/       # 재사용 가능한 애니메이션 컴포넌트
│   └── feature/         # 기능별 복합 컴포넌트
├── contexts/            # React Context 프로바이더
├── hooks/              # 커스텀 훅
├── lib/                # 유틸리티 및 설정
│   ├── design-tokens.ts
│   ├── theme.ts
│   └── animations.ts
├── store/              # Zustand 스토어 (도메인별 분리)
│   ├── session/
│   ├── audio/
│   └── analysis/
└── types/              # 전역 타입 정의
```

### 네이밍 컨벤션
```typescript
// 컴포넌트: PascalCase
export const SessionRecorder = () => {};

// 훅: camelCase with use prefix  
export const useAnalysisController = () => {};

// 타입/인터페이스: PascalCase
export interface SessionState {}

// 상수: UPPER_SNAKE_CASE
export const DEFAULT_ANIMATION_DURATION = 300;

// 파일명: kebab-case
// session-recorder.tsx
// use-analysis-controller.ts
```

## 🔒 보안 & 성능 고려사항

### 보안
- **API 키 관리**: 환경 변수로 관리, 클라이언트 노출 최소화
- **오디오 데이터**: 로컬 처리 우선, 서버 전송 시 암호화
- **사용자 입력 검증**: Zod 스키마로 런타임 검증

### 성능
- **코드 스플리팅**: 라우트별, 기능별 지연 로딩
- **메모이제이션**: React.memo, useMemo, useCallback 적절히 활용
- **번들 크기**: Tree shaking, 사용하지 않는 라이브러리 제거

## 🌐 국제화 (i18n) 준비

### 다국어 지원 구조
```typescript
// src/lib/i18n.ts
export const messages = {
  ko: {
    common: {
      start: '시작하기',
      stop: '중지하기',
      loading: '로딩 중...',
    },
    session: {
      recording: '녹음 중...',
      processing: '분석 중...',
      completed: '분석 완료',
    }
  },
  en: {
    // English translations
  }
};
```

## 📱 PWA 기능 계획

### Service Worker
- **오프라인 지원**: 핵심 기능 캐싱
- **백그라운드 동기화**: 분석 결과 동기화
- **푸시 알림**: 분석 완료 알림

### 매니페스트
```json
{
  "name": "HeartSignal",
  "short_name": "HeartSignal",
  "theme_color": "#E95877",
  "background_color": "#FDF2F8",
  "display": "standalone",
  "orientation": "portrait"
}
```

## 🎊 결론

HeartSignal v1.0은 **SOLID 원칙**과 **재사용성**을 기반으로 한 견고한 아키텍처 위에서 **감정적 연결**과 **직관적 사용성**을 구현하는 것을 목표로 합니다.

### 핵심 가치 실현 방법

1. **"감정은 섬세하게"**
   - 마이크로 애니메이션으로 섬세한 감정 표현
   - 컬러 시스템을 통한 감정 상태 시각화
   - 타이포그래피로 따뜻한 톤앤매너 구현

2. **"결과는 간단하게"**
   - 복잡한 분석 결과를 직관적인 점수로 단순화
   - 실행 가능한 피드백으로 명확한 가이드 제공
   - 불필요한 UI 요소 제거로 집중도 향상

3. **"지속 가능한 코드"**
   - SOLID 원칙으로 유지보수성 확보
   - 컴포넌트 재사용성으로 개발 효율성 증대
   - 타입 안전성으로 런타임 에러 최소화

이 문서는 개발팀이 일관된 품질의 코드를 작성하고, 브랜드 가치를 일관되게 구현할 수 있도록 하는 **나침반** 역할을 합니다.

---
*"Architecture is about the important stuff. Whatever that is." - Ralph Johnson*  
*"좋은 디자인은 보이지 않는다. 단지 작동할 뿐이다." - Dieter Rams*
